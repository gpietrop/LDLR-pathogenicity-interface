# FH LDLR Variant Pathogenicity Classifier

A simple web interface to classify LDLR gene variants as benign or pathogenic using DT.

## How to run

Make sure you have the proper environment set up (see `requirements.txt`), then:

```bash
conda activate fhearly
streamlit run app/app.py
```

And the app will open in your browser automatically
